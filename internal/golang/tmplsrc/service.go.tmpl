{{ .Header }}package service

import (
	"context"

	"{{ .DomainImport }}"
)

// {{ .RepoType }}Iface is the contract service requires from the repository layer.
// The repository must implement this interface — checked at compile time in main.
type {{ .RepoType }}Iface interface {
	{{ .OperationMethod }}(ctx context.Context, req *domain.{{ .RequestType }}) (*domain.{{ .ResponseType }}, error)
}

// {{ .ServiceType }} contains the business logic for {{ .DomainTitle }}.
type {{ .ServiceType }} struct {
	repo {{ .RepoType }}Iface
}

// New{{ .ServiceType }} creates a new {{ .ServiceType }}.
func New{{ .ServiceType }}(repo {{ .RepoType }}Iface) *{{ .ServiceType }} {
	return &{{ .ServiceType }}{repo: repo}
}
